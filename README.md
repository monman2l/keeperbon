# Hello React

极简的 Vite + React “Hello World” 示例，保留了最基本的脚手架与样式，方便直接继续开发。

## 开发

```bash
yarn install
yarn dev
```

## 构建与预览

```bash
yarn build
yarn preview
```

> 项目当前锁定在 Vite 5，兼容 Node.js 18+。如果你升级到 Node.js 20 或 22，可以自行提升 Vite 版本。
